<%= form_for(@link_source) do |f| %>
  <% if @link_source.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@link_source.errors.count, "error") %> prohibited this link_source from being saved:</h2>

      <ul>
      <% @link_source.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :picture %><br>
    <%= f.text_field :picture %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
